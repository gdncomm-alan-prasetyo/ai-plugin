---
description: Take a Jira ticket to a clean PR — plan, test cases, code + tests, PR, CI, review and fix until no findings remain. Never merges.
argument-hint: <JIRA-KEY or browse URL>
---

Invoke the `ai-plugin:jira-to-pr` skill with argument `$ARGUMENTS` and follow it end to end in this session.

No argument → ask for the Jira key first. Run from inside the target service repo.
